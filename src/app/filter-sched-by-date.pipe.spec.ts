import { FilterSchedByDatePipe } from './filter-sched-by-date.pipe';

describe('FilterSchedByDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSchedByDatePipe();
    expect(pipe).toBeTruthy();
  });
});
