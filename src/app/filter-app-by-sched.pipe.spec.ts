import { FilterAppBySchedPipe } from './filter-app-by-sched.pipe';

describe('FilterAppBySchedPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAppBySchedPipe();
    expect(pipe).toBeTruthy();
  });
});
