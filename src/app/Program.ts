export class Program {
    constructor(
        id: number,
        name: string,
        description: string,
        eligibility: string,
        duration: number,
        certificate: string
    ) { }
}
